<head>
    <title>{% block title %}{{ pageTitle }}{% endblock %}</title>
    <meta charset="UTF-8" name="viewport" content="width=device-width, initial-scale=1">
    <script src="/dist/main.js"></script>
</head>
<body class="body">
<div class="card rounded-3 text-black big-margin">
    <h3 class="default-margin-abs">
        {% if action == 'post' %}
            New record successfully created
        {% elseif action == 'update' %}
            Record successfully updated
        {% elseif action == 'delete' %}
            Record(s) successfully deleted
        {% else %}
            How did you get here?
        {% endif %}
    </h3>
    <form method="POST" action="/" class="small-margin-abs">
        <input type="hidden" name="altDataSource" value="{% if altDataSource == true %}true{% else %}false{% endif %}"/>
        <div class="default-margin-abs">
            <button type="submit" class="btn btn-outline-info">Back to main page</button>
        </div>
    </form>
</div>
</body>